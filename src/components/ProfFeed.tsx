import { Professor } from '@prisma/client';
import React from 'react';

type Props = {
  profs: Professor[];
};

const ProfFeed = ({ profs }: Props) => {
  return <div></div>;
};

export default ProfFeed;
